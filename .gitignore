.compound-engineering/*.local.yaml
.claude/settings.local.json
.DS_Store

# Mutable flow cursor (OPEN-1): version the static state-machine.json, not the
# per-session cursor. state.example.json is the tracked template.
flow/state.json
.serena/
# Overnight run artifact (local morning report, not merged)
REPORT.md

# vibe-cli Python package: venv, caches, build output (managed by uv/hatchling)
cli/.venv/
cli/**/__pycache__/
cli/.pytest_cache/
cli/dist/
cli/*.egg-info/

# tooling cache
.tokensave/
