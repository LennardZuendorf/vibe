---
title: "F6 [flow] flow-teeth-hooks: Rust guard+stop hooks, hook.sh shim, 5-tier discovery"
id: F6
issue: 26
tool: flow
phase: 1
depends_on: [F5]
parent: epic
---

## Deliverable
Guard hook (`PreToolUse` on `Edit|Write|NotebookEdit`) and stop gate (`Stop` evidence tooth) ported to Rust. `bin/hook.sh` (POSIX sh, shellcheck-clean, ≤40 lines) + `hooks.json`, invoked as `"${CLAUDE_PLUGIN_ROOT}/bin/hook.sh <event>"`. 5-tier discovery: `$VIBE_FLOW_BIN` → `$CLAUDE_PLUGIN_ROOT/bin/<os>-<arch>/vibe-flow` → `command -v vibe-flow` → `bin/fallback/<event>.sh` (frozen bash, guard/stop only) → `exit 0` with one stderr line, never downloads. `tests/parity/GREEN.hard` marker file; `port-order` CI job fails if `crates/vibe-flow/src/tui/` or `crates/vibe-signals/` exists while `GREEN.hard` is absent.

## Scope
**Owns:** guard/stop hook logic, the shim, the fallback tier.
**Does not own:** `state set` enforcement (F7 — the writer, not the hooks, is the ultimate gate); orders/doctrine (F8).

## Acceptance (evidence, not intent)
- [ ] parity-green on the guard corpus, ≥1 block per policy rule, plus Bash-sniffer warn cases
- [ ] parity-green on the receipt × git-state corpus for stop
- [ ] discovery tier matrix tested with each higher tier's variables actually deleted from the child env (not merely unset in the parent), both legs per tier
- [ ] present-but-corrupt policy on guard/stop → exit 2 (fail-safe branch, pinned with a control case)
- [ ] `bash tests/run.sh` green, `cargo test --workspace` green

## Depends on
F5

## Decisions this implements
- D26 — hook shim and discovery
- D30 — port with frozen oracles, hard units first (guard/stop are the two hard units)

## Notes
- A control case that depends on an unset variable must delete it, not merely not set it — apply this to every discovery-tier test, on the shared spawn helper.
- Where an oracle disagrees with itself across code paths, follow the fail-safe branch (exit 2) and pin the divergence with a control.

Phase: 1 · Tool: flow · Source: `.spec/plan.md` row F6

---
_Generated by [Claude Code](https://claude.ai/code)_
