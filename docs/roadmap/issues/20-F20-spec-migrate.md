---
title: "F20 [spec] spec-migrate: migrate command + this repo's move to docs/spec/"
id: F20
issue: 37
tool: spec
phase: 3
depends_on: [F17, F19]
parent: epic
---

## Deliverable
`vibe-spec migrate --to docs/spec [--dry-run]`: `git mv`, write `root` into `.vibe/spec.json`, rewrite `.spec/` literals in an explicit file set (new root, `content/policy.json`, `.claude/**`, `.agents/**`, `tests/**`, root `*.md`, `flow/reference/templates/**`) with a population assertion (literals-before ≥1, literals-after ==0, files-scanned ≥ floor — else revert and abort), and insert `<!-- vibe-spec: allow SF-nn -->` markers so no warning appears that cannot be acted on today. This repo migrates from `.spec/` to `docs/spec/` in one commit using the tool it ships.

## Scope
**Owns:** the `migrate` command and this repo's one-commit migration.
**Does not own:** the strictness config the allow-markers plug into (F17, already landed); the hooks that will run against the new root (F19, already landed).

## Acceptance (evidence, not intent)
- [ ] `--dry-run` prints the exact rewrite table before any write
- [ ] zero `.spec/` literals remain in the rewritten file set after migration
- [ ] a floor-miss (fewer files scanned than expected) reverts the migration and aborts — tested with a planted-short corpus
- [ ] this repo's full suite (`tests/run.sh`) and `validate.sh` (or its Rust successor) are green post-migration
- [ ] `bash tests/run.sh` green, `cargo test --workspace` green

## Depends on
F17, F19

## Decisions this implements
- D33 — spec root and discovery (migrate is the mechanized transition)
- D35 — strictness ratchet (allow-marker insertion keeps warnings actionable)

## Notes
- A check that examines nothing must fail loudly — the population assertion (before ≥1, after ==0, files ≥ floor) is exactly this lesson applied to a migration script; do not ship migrate without it.
- Warnings the owner cannot act on today must never appear — `migrate` inserting allow-markers itself is the mechanism, not a follow-up task.

Phase: 3 · Tool: spec · Source: `.spec/plan.md` row F20

---
_Generated by [Claude Code](https://claude.ai/code)_
