---
title: "F19 [spec] spec-hooks-agents: Claude/OpenCode auto-validate hooks + 4 registered agents"
id: F19
issue: 36
tool: spec
phase: 3
depends_on: [F16]
parent: epic
---

## Deliverable
Claude Code hooks: `Stop` → `validate --changed` (git-status predicate, fallback mtime-vs-session-marker, 2 s cap, exit 0 always), `PostToolUse` on Edit/Write under the spec root → `validate --file` (≤500 ms). OpenCode `tool.execute.after` shim, same predicate/command. `vibe-spec setup --git-hooks` installs a `pre-commit` template running `validate --changed --strict` — the only hook allowed to exit 1. Four agents registered on both platforms: `spec-interviewer` (sonnet, writes `features/<name>/product.md` only), `spec-tracer` (sonnet, writes plan.md Requirements Trace), `spec-promoter` (sonnet, writes root docs only through the engine), `spec-health` (haiku, read-only, returns findings). Each agent file ≤40 lines, explicit write path under `$(vibe-spec root)`, "binary missing → report and stop." spec's AGENTS.md fragment (≤6 lines).

## Scope
**Owns:** spec's own hooks, the 4 agent definitions on both platforms, spec's AGENTS.md fragment.
**Does not own:** the cursor, injection, or block composition (instruct); the delta engine (F18); the migration itself (F20).

## Acceptance (evidence, not intent)
- [ ] changed-root and unchanged-root fixtures produce the expected `Stop` output
- [ ] a timeout on either hook exits 0 (never blocks the session)
- [ ] `claude plugin validate --strict` passes on the spec plugin dir
- [ ] each of the 4 agent files names its write path and its "binary missing" fallback — asserted by a fixture per agent, not eyeballed
- [ ] `bash tests/run.sh` green, `cargo test --workspace` green

## Depends on
F16

## Decisions this implements
- D25 — hook ownership (spec owns Stop `validate --changed`, PostToolUse `validate --file`)
- D36 — spec subagents are real platform agents

## Notes
- Today's subagents exist as files but are registered nowhere (`.spec/tech.md:141` claims otherwise — false); this issue is what makes the claim true. Fix the code, then fix the comment.
- A comment asserting a safety property becomes load-bearing — "binary missing → report and stop" per agent needs a fixture, not just prose.

Phase: 3 · Tool: spec · Source: `.spec/plan.md` row F19

---
_Generated by [Claude Code](https://claude.ai/code)_
