---
title: "F14 [instruct] instruct-adapters: Claude + OpenCode adapters, .claude/rules emitter, doctor"
id: F14
tool: instruct
phase: 2
depends_on: [F12, F13]
parent: epic
---

## Deliverable
Claude Code plugin: `hooks.json` wiring `hook session-start` (static tiers + one staleness line, never live state) and `hook prompt` (compose); `CLAUDE_PLUGIN_DATA` session state; `PreCompact` resets the session ledger so the next inject is full, not edge-delta. OpenCode shim: `experimental.chat.system.transform`, `chat.message`, `experimental.session.compacting`, `config` hook appending generated instructions. `.claude/rules/vibe-instruct-<id>.md` generated only for repo-shared blocks declaring `paths:`. `init` command (tier scaffolding, `.git/info/exclude`, `@AGENTS.md` creation, existing-symlink preservation). `doctor` + `blocks explain <id>` (which tier won and why).

## Scope
**Owns:** both platform adapters, the rules emitter, `init`, `doctor`.
**Does not own:** the payload/ledger reading logic itself (F12, already landed); source fetching (F13, already landed).

## Acceptance (evidence, not intent)
- [ ] hook JSON stdin fixtures produce the expected stdout for both `session-start` and `prompt`
- [ ] a `--resume` source re-emits the staleness line only, never live state
- [ ] OpenCode shim unit test with a mocked binary confirms `instructions` contains the generated path
- [ ] each of a set of broken-wiring fixtures produces a named `doctor` finding; a clean tree exits 0
- [ ] `bash tests/run.sh` green, `cargo test --workspace` green

## Depends on
F12, F13

## Decisions this implements
- D25 — hook ownership (instruct owns SessionStart + UserPromptSubmit)
- D31 — instruct tiers (`CLAUDE.md`/`@AGENTS.md` handling, `.claude/rules` emission)

## Notes
- SessionStart output replays stale on `--resume` per this repo's validated constraints — the resume fixture is the direct test of that risk; never let live state leak into session-start.
- A check that examines nothing must fail loudly — `doctor`'s clean-tree-exits-0 case only proves something if the broken-wiring fixtures actually produce distinct, named findings first.

Phase: 2 · Tool: instruct · Source: `.spec/plan.md` row F14

---
_Generated by [Claude Code](https://claude.ai/code)_
