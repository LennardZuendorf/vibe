---
title: "F5 [flow] flow-core: port machine/policy/cursor to Rust at .vibe/run/flow/state.json"
id: F5
issue: 25
tool: flow
phase: 1
depends_on: [F3, F4]
parent: epic
---

## Deliverable
Port machine, policy, and glob logic into `vibe-core`; cursor moves to `.vibe/run/flow/state.json`. `vibe-flow state get`, `vibe-flow policy check <path>` commands. `CLAUDE_PROJECT_DIR`-deleted-env control test (the env var must actually be deleted from the child process, not just unset in the parent).

## Scope
**Owns:** cursor read path, `state-machine.json`/`policy.json` parsing, policy decision logic (allow/warn/block).
**Does not own:** the writer/gate (`state set` with `next`/`--confirm` enforcement — F7); guard/stop hooks (F6); orders/doctrine text (F8).

## Acceptance (evidence, not intent)
- [ ] parity-green on `state get` corpus and `policy decide` corpus, including one mutated policy per rule
- [ ] `CLAUDE_PROJECT_DIR` control run with the variable actually deleted from the child env (not merely absent in the parent) — both legs (set/deleted) pass
- [ ] `bash tests/run.sh` green, `cargo test --workspace` green

## Depends on
F3, F4

## Decisions this implements
- D21 — `.vibe/` is the per-repo vibe directory (cursor path)
- D19 — Rust, static, zero runtime deps

## Notes
- A control case that depends on an unset variable must delete it, not merely not set it — the `CLAUDE_PROJECT_DIR` control belongs on the shared spawn helper so every call site inherits the deletion.
- Run this leg in both states (set and deleted) as its own test, not folded into a happy-path case.

Phase: 1 · Tool: flow · Source: `.spec/plan.md` row F5

---
_Generated by [Claude Code](https://claude.ai/code)_
