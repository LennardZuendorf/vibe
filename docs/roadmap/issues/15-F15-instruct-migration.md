---
title: "F15 [instruct] instruct-migration: split Active-Rules block into committed vs global blocks"
id: F15
issue: 32
tool: instruct
phase: 2
depends_on: [F14]
parent: epic
---

## Deliverable
Split today's Active-Rules content: repo-specific lessons stay in the committed block; workflow doctrine, subagent tiers, and output style move to global blocks. Root `vibe.json` `instruct` key read as an alias for one release, then removed. Port `merge-agents.sh` and `regen-active-rules.sh` with file-byte parity against their current output. Retire the old marker grammar (`vibe:instructions` / `vibe:active-rules:*` / `vibe:rules`) in favor of D24's `vibe:<owner>:begin v=1 hash=…` grammar.

## Scope
**Owns:** the migration of this repo's own AGENTS.md content and the old-marker retirement.
**Does not own:** the marker writer itself (F11, already landed) or the adapters that inject the resulting blocks (F14, already landed).

## Acceptance (evidence, not intent)
- [ ] this repo's AGENTS.md passes `write --check` and `lint` post-migration
- [ ] AGENTS.md fixture byte parity against `merge-agents.sh`/`regen-active-rules.sh`'s current output, for the content that is supposed to be unchanged
- [ ] the old marker grammar (`vibe:instructions`, `vibe:active-rules:*`, `vibe:rules`) no longer appears anywhere in the repo post-migration
- [ ] `bash tests/run.sh` green, `cargo test --workspace` green

## Depends on
F14

## Decisions this implements
- D24 — managed-block grammar v1 (retires the old grammar)
- D31 — instruct tiers (committed vs. global split)

## Notes
- A comment asserting a safety property becomes load-bearing — the committed block's closing line ("nothing this repo requires lives outside this block") must be true after this migration, not just asserted; verify by degrading instruct and confirming flow/spec still work from the committed block alone.

Phase: 2 · Tool: instruct · Source: `.spec/plan.md` row F15

---
_Generated by [Claude Code](https://claude.ai/code)_
