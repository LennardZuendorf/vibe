---
title: "F21 [all] packaging: three plugin dirs, marketplace.json, doctor peer checks"
id: F21
issue: 38
tool: all
phase: 4
depends_on: [F8, F14, F19]
parent: epic
---

## Deliverable
`spec/`, `flow/`, `instruct/` each become the actual Claude Code plugin: `.claude-plugin/plugin.json`, `skills/<tool>/`, `commands/`, `agents/`, `hooks/hooks.json`, `bin/hook.sh`, `opencode/index.ts`. No generation step, no `build-plugin.sh`, no symlinked payload. Root `.claude-plugin/marketplace.json` lists all three with `source: ./<tool>`. Installer (F22) also copies skills to `.agents/skills/<tool>/` for neutral readers. Each tool's `doctor` gains peer and contract-major-version checks.

## Scope
**Owns:** the plugin directory structure and manifests, `marketplace.json`, the peer/major-version checks in `doctor`.
**Does not own:** the installer script itself (F22) or the release pipeline that produces the binaries `bin/hook.sh` discovers (F22).

## Acceptance (evidence, not intent)
- [ ] `claude plugin validate --strict` passes on all three plugin directories
- [ ] each `bin/hook.sh` exits 0 with one stderr line when its binary is absent (no crash, no download)
- [ ] fixture environments (binary present / absent / wrong major) each produce the expected `doctor` finding
- [ ] `bash tests/run.sh` green, `cargo test --workspace` green

## Depends on
F8, F14, F19

## Decisions this implements
- D37 — packaging: the tool directory is the plugin
- D20 — contracts are the only coupling (doctor's major-mismatch warning)

## Notes
- Degrade gracefully — a missing binary must never crash a hook; this is the same discovery-tier requirement as F6, now proven for all three tools' shims, not just flow's.

Phase: 4 · Tool: all · Source: `.spec/plan.md` row F21

---
_Generated by [Claude Code](https://claude.ai/code)_
