---
title: "F17 [spec] spec-validators: strictness ratchet, grammar checks, adherence checks, trace"
id: F17
tool: spec
phase: 3
depends_on: [F16]
parent: epic
---

## Deliverable
Substring-match fixes in the ported validators (R-ID prefix, RFC-2119 word boundary, Scope scoping, zero-requirement short-circuit). Strictness config `.vibe/spec.json` `strictness: {"SF-nn": "warn|error|off"}`, default warn; `--strict` promotes non-off to error; a green with zero files examined is refused under `--strict`. Grammar checks (≥1 scenario per requirement, WHEN/THEN present, SHALL/MUST word-bounded) with a unified R-ID scheme. Adherence checks: layout allowlist, frontmatter completeness/enum-validity, `children:` backlinks, feature-folder-name-matches-plan-row, freshness (`updated:` vs. last commit). `vibe-spec trace [--since <ref>]`: cites `<name>/n` units from `git log` and `tests/**`, reports uncited units and orphan citations.

## Scope
**Owns:** all validator logic, strictness config, `trace`.
**Does not own:** the delta/promotion engine and OpenSpec interchange (F18); hook wiring (F19); the actual migration (F20).

## Acceptance (evidence, not intent)
- [ ] one landed mutant per fixed bug (R-ID prefix, RFC-2119 boundary, Scope scoping, zero-requirement short-circuit) makes the corresponding test fail
- [ ] warn/error/off strictness matrix, all three states tested per check
- [ ] OpenSpec sample specs pass grammar checks; mutated samples fail
- [ ] synthetic repo fixture with both cited and uncited units exercises `trace`'s report correctly
- [ ] `bash tests/run.sh` green, `cargo test --workspace` green

## Depends on
F16

## Decisions this implements
- D34 — OpenSpec grammar compatibility (grammar checks)
- D35 — strictness ratchet

## Notes
- A check that examines nothing must fail loudly — `--strict` refusing a zero-files green is the structural floor this repo's lessons call for.
- A check moves to `error` only in the compound that migrates the last live spec — do not flip any check to error in this issue.

Phase: 3 · Tool: spec · Source: `.spec/plan.md` row F17

---
_Generated by [Claude Code](https://claude.ai/code)_
