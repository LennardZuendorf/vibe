---
title: "F1 [spec] spec-path-hotfix: fix hardcoded script paths breaking plugin installs"
id: F1
issue: 23
tool: spec
phase: 0
depends_on: []
parent: epic
---

## Deliverable
Same-week bash hotfix on the current tree (no Rust yet). Rewrite four hardcoded `.agents/skills/spec/scripts/*` references to skill-relative paths: `spec/SKILL.md:319` (auto-exec line), `spec/agents/spec-promoter/SKILL.md:16` and `:19`, `spec/agents/spec-interviewer/SKILL.md:15`, `spec/feature.md:105`. Add self-location (resolve the script's own dir) plus upward marker search to all four spec scripts that currently lack it: `validate.sh`, `list-specs.sh`, `lessons-for.sh`, `scan-merges.sh`. Widen the existing path-guard test from its 4-file/`validate.sh`-literal scan to every `spec/**/*.md` file and every script name in `spec/scripts/`.

## Scope
**Owns:** the four hardcoded references and the four scripts' self-location logic; the widened guard test.
**Does not own:** the Rust port of these scripts (F16), root-discovery config (`spec.json`, `$VIBE_SPEC_ROOT`) — that lands with F16's `vibe-spec root`.

## Acceptance (evidence, not intent)
- [ ] `spec/tests/run.sh` green
- [ ] `cd flow && bash ../spec/scripts/validate.sh` no longer exits 1 ("No .spec/ directory found")
- [ ] running `validate.sh` from a bare tmp dir with only the skill copied no longer exits 127
- [ ] the widened guard test scans every `spec/**/*.md` and every script name, not just 4 files and the literal `validate.sh` — assert file-count floor ≥ actual tree size so an empty scan fails loudly
- [ ] `bash tests/run.sh` green

## Depends on
—

## Decisions this implements
- D33 — spec root and discovery (interim bash version; full order lands in F16)

## Notes
- A check that examines nothing must fail loudly — the widened guard must assert its own population (≥1 file scanned) before it can claim a clean pass.
- A comment asserting a safety property becomes load-bearing — `.spec/tech.md:143-145` claims paths are "named by command, never install-specific"; this is currently false. Fix the code, then fix the comment in the same commit.

Phase: 0 · Tool: spec · Source: `.spec/plan.md` row F1

---
_Generated by [Claude Code](https://claude.ai/code)_
