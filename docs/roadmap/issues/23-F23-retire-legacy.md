---
title: "F23 [all] retire-legacy: delete flow/engine, node tests, old install paths; migrate cursor"
id: F23
tool: all
phase: 4
depends_on: [F15, F20, F22]
parent: epic
---

## Deliverable
Delete `flow/engine/`, node tests, `package.json` bin entry, `build-plugin.sh`, `.claude/hooks/*` (legacy JS-calling versions), old `install.sh` copy-mode paths. `vibe-flow migrate`: moves the cursor from `.agents/skills/vibe/state.json` to `.vibe/run/flow/state.json` and retires the root `vibe.json` alias (read once, then removed per D21). `.agents/skills/<tool>` symlinks repointed to the new plugin-dir skills.

## Scope
**Owns:** the deletion itself and the cursor/alias migration command.
**Does not own:** any of the Rust replacements being deleted-around (all prior features, already parity-green per F3's gate).

## Acceptance (evidence, not intent)
- [ ] full parity suite green immediately before deletion (the `port-order` CI job from F6/F3 is the gate — `tests/parity/GREEN.hard` present)
- [ ] `grep -r node` (or equivalent) finds zero references in any hooks.json or hook shim post-deletion
- [ ] this repo's own fixture state migrates to the new `.vibe/run/flow/state.json` layout with block-content equality to the pre-migration cursor
- [ ] `bash tests/run.sh` green, `cargo test --workspace` green

## Depends on
F15, F20, F22

## Decisions this implements
- D30 — port with frozen oracles (JS deleted only when every unit is parity-green — this issue is the deletion gate)
- D21 — `.vibe/` is the per-repo vibe directory (cursor migration)

## Notes
- Porting a script destroys the oracle that proves the port — the oracles in `oracles/js/` and `oracles/bash/` stay in tree permanently even after this deletion; do not delete them.
- Nothing merges past this gate until parity is green — confirm `tests/parity/GREEN.hard` exists before running the deletion, not after.

Phase: 4 · Tool: all · Source: `.spec/plan.md` row F23

---
_Generated by [Claude Code](https://claude.ai/code)_
