---
title: "F24 [all] doc-truth: READMEs, CHANGELOG, root spec truth sweep across 3 tools"
id: F24
issue: 41
tool: all
phase: 4
depends_on: [F23]
parent: epic
---

## Deliverable
READMEs (umbrella repo + one per tool), CHANGELOG, root `.spec`/`docs/spec` truth sweep — every claim marked "shipped today" vs. "v0.4 target" gets reconciled to just "true." `check-drift` (F18) extended to run across all three tools' documentation, not just spec's own tree.

## Scope
**Owns:** documentation content and the drift check's three-tool extension.
**Does not own:** any remaining code changes — this issue is a truth sweep against what F1–F23 actually shipped, not new functionality.

## Acceptance (evidence, not intent)
- [ ] `check-drift` runs green across spec, flow, and instruct documentation
- [ ] a stranger-eval pass: a fresh clone with no git history and no prior context can follow the umbrella README to install and use each tool independently
- [ ] `bash tests/run.sh` green, `cargo test --workspace` green

## Depends on
F23

## Decisions this implements
- D39 — doctrine stays: enforce in code, explain in prose, never both; data over prose

## Notes
- A comment asserting a safety property becomes load-bearing — every "shipped today vs. v0.4 target" marker in the specs must be re-verified against the actual repo state at this point, not carried forward from when it was written.

Phase: 4 · Tool: all · Source: `.spec/plan.md` row F24

---
_Generated by [Claude Code](https://claude.ai/code)_
