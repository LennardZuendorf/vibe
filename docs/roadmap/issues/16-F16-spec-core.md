---
title: "F16 [spec] spec-core: vibe-spec crate: root discovery, port validate/list/lessons"
id: F16
issue: 33
tool: spec
phase: 3
depends_on: [F3, F4]
parent: epic
---

## Deliverable
`vibe-spec` crate. Root discovery order: `--root` → `$VIBE_SPEC_ROOT` → `.vibe/spec.json` `root` → `docs/spec/` → `.spec/` → repeat per parent up to `.git` → exit 3. `vibe-spec root [--json]` is the one function every subcommand and peer calls. Freeze the spec bash scripts as oracles under `tests/oracle/` (companion to F3's flow oracles). Port `validate`, `list-specs`, `lessons-for`, `scan-merges`. JSON queries for peers: `lessons-for --json`, `plan --json`, `feature --json` (exit 0 + JSON, exit 3 = no spec tree).

## Scope
**Owns:** root discovery, the four ported scripts, JSON query surface.
**Does not own:** strictness/grammar/adherence checks (F17); delta/OpenSpec interchange (F18); hooks/agents (F19); the actual migration to `docs/spec/` (F20).
**Reads from peers:** nothing. **Writes for peers:** the JSON query answers flow's degrade path (F8) reads.

## Acceptance (evidence, not intent)
- [ ] oracle diff clean against `tests/oracle/` + population floor (≥1 spec file scanned)
- [ ] invocation from a subdirectory resolves the same root as invocation from repo root
- [ ] exit 0/3 goldens (tree present / absent) pass
- [ ] `bash tests/run.sh` green, `cargo test --workspace` green

## Depends on
F3, F4

## Decisions this implements
- D33 — spec root and discovery

## Notes
- Porting a script destroys the oracle that proves the port — freeze `tests/oracle/` before porting a single script in this issue.
- A check that examines nothing must fail loudly — `validate` must assert a floor on files examined, not just report zero findings as success.

Phase: 3 · Tool: spec · Source: `.spec/plan.md` row F16

---
_Generated by [Claude Code](https://claude.ai/code)_
