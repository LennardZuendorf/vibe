---
title: "F3 [flow] oracles-parity: freeze JS/bash oracles, build differential parity harness"
id: F3
tool: flow
phase: 0
depends_on: []
parent: epic
---

## Deliverable
Freeze today's implementation as permanent oracles before any Rust line is written: `oracles/js/` (verbatim copy of `flow/engine/`), `oracles/bash/` (every current script including both hook fallbacks), `oracles/MANIFEST.sha256` checked in CI. A generated corpus (`tests/parity/corpus/`) with structural floors derived from `state-machine.json`/`policy.json` (cursors = states × {feature, none, malformed, missing}; policies = real + one mutation per rule; stop payloads = receipt × git-state cross product). `crates/vibe-parity`: differential harness diffing oracle vs. candidate exit code, stdout bytes, and resulting file bytes.

## Scope
**Owns:** the oracle freeze, corpus generator, parity harness, `expected-divergence` pin mechanism.
**Does not own:** the Rust candidate implementations themselves (F5–F10 write those); the harness only compares.

## Acceptance (evidence, not intent)
- [ ] CI turns red on any byte change to a frozen oracle file
- [ ] a planted Rust mutant (confirmed landed via `git diff --numstat` before trusting the result) turns the parity harness red
- [ ] an empty corpus class fails the run (population floor, not just "zero failures")
- [ ] `bash tests/run.sh` green

## Depends on
—

## Decisions this implements
- D30 — port with frozen oracles, hard units first

## Notes
- Porting a script destroys the oracle that proves the port — freeze `oracles/js/` and `oracles/bash/` in this issue, before F5 touches a single line of Rust. Blast-radius order: guard and stop are hard units, frozen and ported first.
- A check that examines nothing must fail loudly — corpus floors must be asserted, not just "no diffs found."
- Bugs found in the JS oracle during parity become pinned `expected-divergence` cases fixed only in Rust, never by editing the oracle.

Phase: 0 · Tool: flow · Source: `.spec/plan.md` row F3

---
_Generated by [Claude Code](https://claude.ai/code)_
