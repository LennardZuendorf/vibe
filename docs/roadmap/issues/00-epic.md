---
title: "v0.4 — three tools, one language, no bundle (tracking)"
id: epic
tool: all
phase: 0-4
depends_on: []
---

vibe is three small, independent tools for coding with agents — **spec** (durable planning memory), **flow** (a state-machine workflow with real teeth), and **instruct** (layered, budgeted instruction injection) — each a single static Rust binary with no runtime dependency, each a Claude Code plugin and an OpenCode plugin, each installable and useful alone, composing through file-based contracts when co-installed. One repo, one installer, three release trains. The repo's own memory stays committed Markdown readable by any agent or none.

**Tagline:** three tools, one language, no bundle.

## Why

1. **Bundled tooling.** Today's `vibe` umbrella couples spec, flow, and instruct-style injection into one plugin — install one, get all three, whether wanted or not.
2. **The Node runtime dependency.** `flow/engine/` requires Node on every machine that runs a hook; a missing or wrong-version Node silently degrades or breaks guard/stop enforcement.
3. **Honour-system gates.** `set-state.sh` writes the cursor but does not enforce `next`/gates; approvals live only in `/flow` prose and hook wiring that can be absent.

## Phase overview

- **Phase 0 — foundations:** contracts, oracles/parity harness, Cargo workspace + `vibe-core` (F2–F4), plus a same-week bash hotfix for the spec path bug (F1).
- **Phase 1 — flow:** Rust port of cursor/machine/policy, guard+stop hooks, machine-teeth (`state set` enforces `next`/gates), orders/payload, signals, TUI (F5–F10).
- **Phase 2 — instruct:** 4-tier layering, block/channel rendering, providers, sources/sync, platform adapters, migration of today's Active-Rules block (F11–F15).
- **Phase 3 — spec:** root discovery at `docs/spec/`, ported validators, strictness ratchet, OpenSpec delta/interchange, hooks + 4 real subagents, this repo's migration (F16–F20).
- **Phase 4 — distribution + retirement:** three plugin packages, `install.sh` + cargo-dist release pipeline, deleting the JS/bash runtime, doc truth sweep (F21–F24).

## Superseded plan rows

Old `plan.md` rows 15–20 are superseded by this sequence: machine-teeth→F7, plugin-runtime→F21/F22, spec-js→F16/F17, delegation-redirect→F9, spec-delta→F18, doc-truth→F24.

<!-- checklist filled in by create-issues.sh -->

---
_Generated by [Claude Code](https://claude.ai/code)_
