---
title: "F8 [flow] flow-orders-payload: port orders/doctrine/doctor, write inject payload+manifest"
id: F8
tool: flow
phase: 1
depends_on: [F7]
parent: epic
---

## Deliverable
Port `orders`, `doctrine`, `doctor` commands. Payload writer: `.vibe/run/inject/flow.json` atomic write, `{v:1, provider:"flow", seq, level, edge, session, events:[]}` — rewrites `level`/`edge`/`session` and bumps `seq` on every `state set`; guard/stop hooks append `events`. `vibe-flow emit` command. Provider manifest written at `init` (`.vibe/providers/flow.json` pointing at `["vibe-flow","emit"]`, payload file as fallback) so per-turn drift nudges work without instruct learning tool names. Standalone AGENTS.md block + policy-derived rendering. Degrade path: when `vibe-spec` is absent or the spec root doesn't exist, flow's orders cite the path pointer only, no lessons.

## Scope
**Owns:** orders/doctrine/doctor logic, the payload writer, the provider manifest, flow's AGENTS.md fragment.
**Does not own:** how instruct reads/composes the payload (F12); the signals that feed advisory events (F9, writes into the same `events` array but is a separate feature).

## Acceptance (evidence, not intent)
- [ ] parity-green per command (`orders`, `doctrine`, `doctor`) against the frozen oracles
- [ ] golden payload snapshot after a transition — `seq` bumped, `level`/`edge`/`session` match expected content
- [ ] `render agents-md --write` run twice produces byte-identical output (idempotency)
- [ ] PATH-less spec probe (no `vibe-spec` binary) degrades to path-pointer-only orders, verified by fixture
- [ ] `bash tests/run.sh` green, `cargo test --workspace` green

## Depends on
F7

## Decisions this implements
- D22 — provider payload contract (flow → instruct)
- D25 — hook ownership (flow has no prompt hooks; standalone block drives adherence)

## Notes
- Degrade gracefully — the PATH-less spec probe must be a tested fixture, not an assumption; missing peers must warn, never break the session.
- A comment asserting a safety property becomes load-bearing — "flow never parses spec markdown" (B2 in the Fable contract doc) needs a test that the flow crate has no markdown-parsing dependency on spec's document model.

Phase: 1 · Tool: flow · Source: `.spec/plan.md` row F8

---
_Generated by [Claude Code](https://claude.ai/code)_
