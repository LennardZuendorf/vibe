---
title: "F9 [flow] flow-signals-hooks: vibe-signals + RulesProvider, Task redirect, ledger hooks"
id: F9
hold: architecture
tool: flow
phase: 1
depends_on: [F8]
parent: epic
---

> **Held.** This feature is under architecture design in the originating thread; no GitHub issue yet.

## Deliverable
`vibe-signals` crate: `Signal{ts, source, kind: progress|blocked|done|drift, confidence, evidence, state}` — no verdict field. `trait SignalProvider { fn observe(&self, &Observation) -> Vec<Signal> }`. `vibe-signals` depends on `vibe-core`; core cannot depend on signals — cycle-free, asserted by a `cargo metadata` test, so guard/gate are structurally unable to read them (signals are advisory by construction, never gating). Ships now: `RulesProvider` (keyword rules, fixed 0.3 confidence). `Observation` built from the transcript tail at `Stop`/`SubagentStop`. New hooks: `PostToolUse(Task)` delegation redirect (advisory `additionalContext` when `model` is missing or the target is outside the state's `delegates`), `SubagentStop` and `SessionEnd` ledger events.

## Scope
**Owns:** the `vibe-signals` crate, `RulesProvider`, the three new hooks.
**Does not own:** the ledger primitive itself (already in `vibe-core` via F4/F5); the TUI's signals pane (F10, reads this feature's output).

## Acceptance (evidence, not intent)
- [ ] `cargo metadata` test asserts `vibe-core` has no dependency edge on `vibe-signals` — this is the structural proof that guard/gate cannot read signals, not a comment
- [ ] transcript fixtures produce the expected `Signal` set from `RulesProvider`
- [ ] a fixture missing `model` or naming a non-delegate target produces the expected `additionalContext` from the Task redirect hook
- [ ] `bash tests/run.sh` green, `cargo test --workspace` green

## Depends on
F8

## Decisions this implements
- D28 — ledger + advisory signals

## Notes
- A comment asserting a safety property becomes load-bearing — "signals never gate" (R14) is exactly the kind of claim this repo's lessons say must be structurally enforced (the dependency-cycle test), not merely documented.

Phase: 1 · Tool: flow · Source: `.spec/plan.md` row F9

---
_Generated by [Claude Code](https://claude.ai/code)_
