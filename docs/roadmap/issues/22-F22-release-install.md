---
title: "F22 [all] release-install: cargo-dist 5 targets, per-tool tags, install.sh lifecycle"
id: F22
issue: 39
tool: all
phase: 4
depends_on: [F21]
parent: epic
---

## Deliverable
cargo-dist targets: `{x86_64,aarch64}-unknown-linux-musl`, `{x86_64,aarch64}-apple-darwin`, `x86_64-pc-windows-msvc`; `SHA256SUMS`. Independent per-tool tags: `spec-v*`, `flow-v*`, `instruct-v*`; first Rust releases `flow-v0.4.0`, `spec-v0.4.0`, `instruct-v0.1.0`. `install.sh` (POSIX sh, `curl | sh -s -- …`): `--only spec,flow,instruct`, `--platform claude,opencode,none`, `--bin-dir`, `--version`, `--dry-run`, `--uninstall`, `--from-source`. Steps: detect triple → fetch `vibe-<tool>-<ver>-<triple>.tar.gz` + `SHA256SUMS` → verify → `~/.local/bin` → fallback `cargo install --locked` → register plugins (Claude marketplace add + `claude plugin install`; OpenCode shim link). `~/.vibe/installed.json` records actions; `--uninstall` reverses only recorded actions.

## Scope
**Owns:** the release pipeline and the installer script.
**Does not own:** the plugin directory contents themselves (F21, already landed) or the binaries' internal logic (all prior features).

## Acceptance (evidence, not intent)
- [ ] a `flow-v0.4.0-rc` tag yields 5 verified artifacts (SHA256 matches)
- [ ] containerised matrix: 7 subsets (`--only` combinations) × 3 platforms, install then verify wiring
- [ ] `--uninstall` restores the pre-install state exactly (diffed against `installed.json`'s recorded actions, nothing beyond them)
- [ ] `bash tests/run.sh` green, `cargo test --workspace` green

## Depends on
F21

## Decisions this implements
- D38 — one installer, three release trains

## Notes
- A check that examines nothing must fail loudly — the uninstall test must assert the recorded-actions list is non-empty before trusting a clean restore; an installer that recorded nothing would "pass" a naive diff.

Phase: 4 · Tool: all · Source: `.spec/plan.md` row F22

---
_Generated by [Claude Code](https://claude.ai/code)_
