---
title: "F13 [instruct] instruct-sources: dir/git/https sources, pins, cache, TTL, lockfile, sync"
id: F13
tool: instruct
phase: 2
depends_on: [F11]
parent: epic
---

## Deliverable
`sources: [{type: dir|git|https, path|url, ref, commit?, sha256?, ttl, scope: global|repo}]`. Repo-scope sources must pin (`commit` or `sha256`) or the loader refuses. Cache under `~/.cache/vibe/instruct/`. `vibe-instruct sync`: fetch, verify pins, update `instruct.lock.json`, rewrite generated files. Offline: keep cache, one warning line, exit 0. Session-start staleness check: no network, no subprocess, ≤200 ms, emits one line or nothing.

## Scope
**Owns:** source fetch/cache/pin/TTL/lockfile, `sync`, the staleness check.
**Does not own:** what happens with fetched content beyond blocks/toggles (fetched providers/hooks/paths outside the source's cache dir are ignored with a warning — data, never code); platform adapter wiring (F14).

## Acceptance (evidence, not intent)
- [ ] an unpinned repo-scope source is refused (loader exits non-zero, no fetch attempted)
- [ ] offline `sync` exits 0 with cache intact and exactly one warning line
- [ ] timed run under a blocked network completes the staleness check within 200 ms with zero network/subprocess calls
- [ ] fetched `instruct.json` providers/hooks/out-of-cache paths are ignored with a warning, never executed
- [ ] `bash tests/run.sh` green, `cargo test --workspace` green

## Depends on
F11

## Decisions this implements
- D32 — sources and sync

## Notes
- A guard is only as strong as the capability it bans — "fetched content is data" must be enforced structurally (fetched config's providers/hooks/paths are parsed but never executed), not merely undocumented.

Phase: 2 · Tool: instruct · Source: `.spec/plan.md` row F13

---
_Generated by [Claude Code](https://claude.ai/code)_
